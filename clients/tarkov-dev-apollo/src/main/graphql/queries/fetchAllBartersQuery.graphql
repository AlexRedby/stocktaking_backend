query fetchAllBartersQuery {
    barters(gameMode: pve) {
        id
        requiredItems {
            ...ContainedItem
        }
        rewardItems {
            ...ContainedItem
        }
        trader {
            id
            name
            imageLink
        }
        level
    }
}

query fetchAllBartersQuery {
    barters(gameMode: pve) {
        id
        requiredItems {
            ...ContainedItem
        }
        rewardItems {
            ...ContainedItem
        }
    }
}

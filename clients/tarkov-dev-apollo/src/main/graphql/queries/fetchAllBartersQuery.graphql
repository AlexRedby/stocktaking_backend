query fetchAllBartersQuery {
    barters(gameMode: pve) {
        id
        requiredItems {
            ...ContainedItem
        }
        rewardItems {
            ...ContainedItem
        }
        trader {
            name
            imageLink
        }
        level
    }
}

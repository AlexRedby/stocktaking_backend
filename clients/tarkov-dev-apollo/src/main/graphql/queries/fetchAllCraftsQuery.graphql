query fetchAllCraftsQuery {
    crafts(gameMode: pve) {
        id
        requiredItems {
            ...ContainedItem
        }
        rewardItems {
            ...ContainedItem
        }
        station {
            id
            name
            imageLink
        }
        level
    }
}

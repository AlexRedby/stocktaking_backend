query fetchAllCraftsQuery {
    crafts(gameMode: pve) {
        id
        requiredItems {
            ...ContainedItem
        }
        rewardItems {
            ...ContainedItem
        }
        station {
            name
            imageLink
        }
        level
    }
}

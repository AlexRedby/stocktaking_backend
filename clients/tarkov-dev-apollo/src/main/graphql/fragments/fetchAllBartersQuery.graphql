fragment ContainedItem on ContainedItem {
    item {
        id
        name
        shortName
        iconLink
    }
    count
    quantity
    attributes {
        type
        name
        value
    }
}

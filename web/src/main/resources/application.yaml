ktor:
  deployment:
    port: 8080
  application:
    modules:
      - ru.alexredby.stocktaking.ApplicationKt.module

database:
  host: localhost
  port: 5432
  name: stocktaking
  user: stocktaking
  password: ${DB_PASSWORD}
  cp:
    maximumPoolSize: 10
    minimumIdle: 2
    connectionTimeout: 10000
    idleTimeout: 600000
    maxLifetime: 1800000
